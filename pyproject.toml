[project]
name = "wechat-local-platform-adapter"
version = "0.1.2"
description = "Fail-closed stdio MCP adapter for a pinned Windows WeChat read engine"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
